server.port=${PORT:5000}

# Data Source
spring.datasource.url=jdbc:postgresql://jkgranites-db.c9auwia6cziw.eu-west-2.rds.amazonaws.com:5432/jkgranites_db
spring.datasource.username=postgres
spring.datasource.password=VigneshSabapathi
spring.datasource.hikari.maximum-pool-size=5
spring.jpa.hibernate.ddl-auto=none
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.show-sql=false

# --- App settings ---
app.products.first-count=${APP_PRODUCTS_FIRST_COUNT:15}
app.products.max-all=${APP_PRODUCTS_MAX_ALL:125}

# S3 location used to build image URLs (frontend will fetch directly)
app.storage.s3.bucket=memorials-images-prod
app.storage.s3.region=eu-west-2


management.endpoints.web.exposure.include=health,info
management.endpoint.health.probes.enabled=true
management.endpoint.health.show-details=never
management.health.livenessstate.enabled=true
# (optional but useful)
server.forward-headers-strategy=framework

app.storage.mode=LOCAL
app.storage.local.path=uploads


app.s3.bucket=memorials-images-prod
app.s3.region=eu-west-2
app.storage.s3.endpoint=

app.auth.mode=DEV
app.admin.token=changeme


spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:kngvky@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:Sabapathi3@}
spring.mail.properties.mail.smtp.auth=${SPRING_MAIL_PROPERTIES_MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLE:true}

# Inquiry config
app.inquiry.mail.to=${APP_INQUIRY_MAIL_TO:1982jkgranites@gmail.com}
app.inquiry.mail.from=${APP_INQUIRY_MAIL_FROM:kngvky@gmail.com}
